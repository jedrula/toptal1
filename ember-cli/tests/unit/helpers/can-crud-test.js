import { canCrud } from '../../../helpers/can-crud';
import { module, test } from 'qunit';

module('Unit | Helper | can crud');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = canCrud([42]);
  assert.ok(result);
});
