import { entryColor } from '../../../helpers/entry-color';
import { module, test } from 'qunit';

module('Unit | Helper | entry color');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = entryColor([42]);
  assert.ok(result);
});
